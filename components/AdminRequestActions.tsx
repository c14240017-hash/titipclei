"use client";
import { useRouter } from "next/navigation";
import { useState } from "react";
export function AdminRequestActions({ id, status }: { id: string; status: string }) { const router = useRouter(); const [saving, setSaving] = useState(false); const [error, setError] = useState(""); async function update(next: string) { setSaving(true); setError(""); const response = await fetch(`/api/admin/requests/${id}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ status: next }) }); setSaving(false); if (!response.ok) return setError("Gagal memperbarui request."); router.refresh(); } return <div className="mt-6 space-y-3"><div className="flex flex-wrap gap-2">{status === "PENDING_REVIEW" && <button disabled={saving} onClick={() => update("REVIEWED")} className="rounded-xl bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white">{saving ? "Memproses..." : "Mulai Cek"}</button>}{status !== "CANCELLED" && status !== "ACCEPTED" && <button disabled={saving} onClick={() => update("CANCELLED")} className="rounded-xl border border-rose-200 px-4 py-2.5 text-sm font-semibold text-rose-700">Tolak Request</button>}</div>{error && <p className="text-sm text-rose-600">{error}</p>}</div>; }
