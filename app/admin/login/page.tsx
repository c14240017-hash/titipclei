"use client";
import Image from "next/image"; import { useState } from "react"; import { useRouter } from "next/navigation"; import { signIn } from "next-auth/react"; import Link from "next/link"; import { LockKeyhole } from "lucide-react"; import { brand } from "@/lib/brand";
export default function AdminLoginPage() { const router = useRouter(); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); const submit = async (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); setLoading(true); setError(""); const data = new FormData(event.currentTarget); const result = await signIn("credentials", { redirect: false, email: data.get("email"), password: data.get("password") }); setLoading(false); if (result?.error) setError("Email atau password admin salah."); else { router.push("/admin"); router.refresh(); } }; return <main className="grid min-h-screen place-items-center bg-[#FFF9F5] p-4"><section className="w-full max-w-md rounded-3xl border border-[#EADBD4] bg-[#FFFDFC] p-8 shadow-[0_8px_30px_rgba(94,64,57,.09)]"><Link href="/" className="relative block h-16 w-52"><Image src={brand.logo} alt="Titip Clei" fill className="object-contain object-left" priority /></Link><p className="mt-2 text-sm font-semibold text-[#90756E]">Admin Panel</p><span className="mt-8 grid h-11 w-11 place-items-center rounded-xl bg-[#F8E8EB] text-[#C66C7C]"><LockKeyhole className="h-5 w-5" /></span><h1 className="mt-4 text-2xl font-bold text-[#3B2925]">Login Admin</h1><p className="mt-2 text-[#90756E]">Masuk untuk mengelola Titip Clei.</p><form onSubmit={submit} className="mt-7 space-y-4"><label className="block text-sm font-semibold text-[#5E4039]">Email<input required name="email" type="email" className="field mt-2" placeholder="admin@titipclei.com" /></label><label className="block text-sm font-semibold text-[#5E4039]">Password<input required name="password" type="password" className="field mt-2" placeholder="••••••••" /></label>{error && <p className="rounded-xl bg-rose-50 p-3 text-sm text-rose-700">{error}</p>}<button disabled={loading} className="h-12 w-full rounded-xl bg-[#D98591] font-semibold text-white hover:bg-[#C66C7C] disabled:opacity-60">{loading ? "Memeriksa..." : "Masuk ke Dashboard"}</button></form></section></main>; }
